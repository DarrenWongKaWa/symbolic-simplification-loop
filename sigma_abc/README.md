# sigma_abc

This project is the future full tensorial conductivity workflow.

Current branch:

```text
sigma_abc_000_raw_contract_and_generation
```

Current status:

```text
RawSigmaABCExists -> False
RawContractExists -> True
RawGenerationNeeded -> True
SigmaXXXBenchmarkRegistered -> True
```

This branch defines the contract and source plan for `raw/raw_sigma_abc.wl`.
It does not generate the raw expression and does not start symbolic
simplification.

