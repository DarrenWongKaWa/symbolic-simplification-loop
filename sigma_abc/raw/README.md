# raw/

This folder is reserved for the future raw tensorial expression:

```text
raw_sigma_abc.wl
```

Current status:

```text
RawSigmaABCExists -> False
RawGenerationNeeded -> True
```

The current branch intentionally does not create `raw_sigma_abc.wl`. It creates
only a contract and a placeholder template:

```text
RAW_SIGMA_ABC_CONTRACT.md
raw_sigma_abc.template.wl
RAW_SOURCE_OPTIONS.md
RAW_SOURCE_MANIFEST.md
RAW_GENERATION_PLAN.md
```

No downstream symbolic simplification may start until a real
`raw_sigma_abc.wl` is generated or transcribed and validated against this
contract.

