# Expected Projection Benchmark

The full tensorial project must satisfy:

```text
sigma_{mu alpha beta}|_{xxx} - sigma^{xxx}_{final} = 0
```

If exact symbolic equality is unavailable, a numerical projection regression may be accepted only as a caveated gate.

