# Loop 012 Engineering Hygiene Before Long sigma_abc Run

## Status

`PASS` as an engineering hygiene patch for longer repo-native `sigma_abc` autonomous runs.

This branch did not modify sigma_abc physics, did not start 012C promotion, did not start Stage 013, did not start tensorial IBP, and did not introduce total-derivative reduction.

## Files Changed

- `agents/runtime.local.yaml`
  - Replaced hard-coded local absolute runner paths with `${REPO_ROOT}/scripts/local_codex_agent_runner.sh`.
- `loop_engine/agent_runtime.py`
  - Added runtime command expansion for `${REPO_ROOT}` / `{repo_root}` before command availability checks and invocation.
- `loop_engine/schemas.py`
  - Added robust schema path resolution from repo root, source-tree fallback, and packaged `schemas` resources.
- `schemas/__init__.py`
  - Marks schema directory as package data container.
- `schemas/review_debt.schema.json`
  - Added `PENDING_REVIEW_RESUME` as a structured debt stage status.
- `pyproject.toml`
  - Added setuptools package-data entry for `schemas/*.schema.json`.
- `loop_engine/review_debt.py`
  - Runtime-limited review debt settlement now returns `PENDING_REVIEW_RESUME` instead of immediately marking debt blocking.
- `scripts/settle_review_debt.py`
  - Added `--project-root` and `--auto-resume`; auto-resume performs reviewer-only resume before debt settlement.
- `scripts/run_stage.py`
  - Added explicit boundary docstring: state-only helper, not full physical verification.
- `README.md`
  - Documented `run_stage.py` boundary and `.loop/reviewer_results/` canonical reviewer-output directory.
- `tests/test_loop012_engineering_hygiene.py`
  - Added Loop 012 hygiene regression tests.
- `tests/test_loop011_l1_reviewer_timeout_debt.py`
  - Updated one legacy test so timeout debt now expects resume, while real non-runtime review failure still blocks promotion.

## Tests Added

- runtime config contains no hard-coded user path and uses `${REPO_ROOT}`.
- schema resolver works after `REPO_ROOT` is moved away from the source tree.
- schema JSON files are included as package data.
- `settle_review_debt` handles timeout/no-output review results safely.
- `settle_review_debt --auto-resume` can settle L1 timeout debt without executor/verifier rerun.
- autonomous report identity labels remain present.
- `run_stage.py` boundary docstring is present.
- README documents reviewer output directory convention.

## Verification

```text
python3 -m pytest -q
136 passed, 1 warning in 19.60s
```

```text
python3 -m compileall loop_engine scripts tests
PASS
```

```text
python3 scripts/check_agent_runtime.py --profile sigma_abc_hypothesis_pre_ibp
AgentRuntimeStatus -> AVAILABLE
Adapter -> command
ProductionRunAllowed -> True
StubUsed -> False
MissingAgentCommands -> []
```

```text
python3 scripts/check_agent_runtime.py --profile sigma_abc_hypothesis_pre_ibp_throughput
AgentRuntimeStatus -> AVAILABLE
Adapter -> command
ProductionRunAllowed -> True
StubUsed -> False
MissingAgentCommands -> []
```

## sigma_abc Boundary Check

Current forbidden stage directories were checked:

```text
sigma_abc_012c_loop_orbit_canonicalization_promotion -> False
sigma_abc_013_global_pre_ibp_assembly -> False
sigma_abc_tensorial_ibp_reduction -> False
```

No 012C promotion, Stage 013 global assembly, tensorial IBP, or total-derivative reduction artifact was generated by this hygiene patch.

The permanent caveat remains preserved:

```text
DCProjectionTo1D -> INHERITED_PASS, not direct full tensorial DC-series PASS.
```

## Recommended Next Command

Run the preparation gate again before any promotion:

```bash
python3 scripts/run_autonomous_loop.py \
  --project sigma_abc \
  --profile sigma_abc_loop_candidate_preparation \
  --from-current-checkpoint \
  --auto-patch \
  --write-digests \
  --max-stages 1
```

Expected target:

```text
PreparationStatus -> READY_FOR_012C_PROMOTION_RETRY
OverallGate -> PASS
NoCandidatePromoted -> True
NoIBPStarted -> True
NoTotalDerivativeIntroduced -> True
```
