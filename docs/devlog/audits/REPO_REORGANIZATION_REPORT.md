# Repo Reorganization Report

## Status

REPO_ROOT_CLEANED.

```text
Final classification (per user spec):
A. "Repo root cleaned; core framework, devlogs, generated artifacts,
   and sigma_abc case study are separated; tests pass."
```

Loop behavior unchanged. `sigma_abc` physics unchanged. No 012C
promotion, Stage 013, tensorial IBP, or total-derivative reduction
started. No full tensorial sigma_abc correctness claim.

Permanent caveat preserved:

```text
DCProjectionTo1D -> INHERITED_PASS, not direct full tensorial DC-series PASS.
```

## Files Moved

33 files into `docs/devlog/loop_engine/`:

```text
LOOP_003_AUTONOMOUS_PROJECT_RUNNER_REPORT.md
LOOP_004_META_REVIEW_AND_DIGEST_REPORT.md
LOOP_005_REAL_AGENT_INVOCATION_REPORT.md
LOOP_006_HYPOTHESIS_DRIVEN_SYMBOLIC_SEARCH_REPORT.md
LOOP_007_LOCAL_RUNTIME_CONFIGURATION_REPORT.md
LOOP_007_REAL_SUBAGENT_RUNTIME_ADAPTER_REPORT.md
LOOP_007_RUNTIME_FREEZE_REPORT.md
LOOP_008_REVIEW_BUDGET_AND_ASYNC_QUEUE_REPORT.md
LOOP_009_REVIEW_QUALITY_HARDENING_REPORT.md
LOOP_010_REVIEW_DEBT_AND_THROUGHPUT_MODE_REPORT.md
LOOP_011_L1_REVIEWER_TIMEOUT_AND_BACKGROUND_DEBT_REPORT.md
LOOP_012_ENGINEERING_HYGIENE_BEFORE_LONG_SIGMA_ABC_RUN_REPORT.md
LOOP_013_COMPLETION_MATRIX_STATUS_HOTFIX_REPORT.md
LOOP_013_HOTFIX_FOLLOWUP_TEST_FIX_REPORT.md
LOOP_013_HUMAN_SIGNOFF_COMPLETION_MATRIX_REPORT.md
LOOP_013_POST_AUDIT_REPORT.md
LOOP_014_AGENT_PRE_RUN_BRIEF_LITE_REPORT.md
LOOP_014_017_FINAL_INTEGRATION_REPORT.md
LOOP_014_017_SIGMA_ABC_DRY_RUN_REPORT.md
LOOP_014_017_STATUS_AUDIT.md
LOOP_015_AGENT_PRE_RUN_BRIEF_HARDGATE_REPORT.md
LOOP_016_SCIENTIFIC_IDENTITY_RENDERING_REPORT.md
LOOP_017_FORMULA_TO_CHECK_TRACEABILITY_REPORT.md
LOOP_018_STAGE_012C_PROMOTION_REPORT.md
LOOP_019_UPSTREAM_012A_012B_MATERIALIZATION_PRE_AUDIT.md
LOOP_019_UPSTREAM_012A_012B_MATERIALIZATION_REPORT.md
LOOP_019R_MATERIALIZATION_RUN_REPORT.md
LOOP_019R_PRE_RETRY_SNAPSHOT.md
LOOP_019R_QUOTA_STILL_ACTIVE_REPORT.md
LOOP_019R_RUNTIME_AND_PRERUN_GATE_REPAIR_PRE_AUDIT.md
LOOP_019R_RUNTIME_AND_PRERUN_GATE_REPAIR_REPORT.md
LOOP_REVIEW_AUTOMATION_REPORT.md
LOOP_SMOKE_TEST_REPORT.md
```

15 files into `docs/devlog/sigma_abc/`:

```text
SIGMA_ABC_012AB_ARTIFACT_CONTRACT_PATCH_REPORT.md
SIGMA_ABC_012C_HUMAN_REVIEW_CHECKLIST.md
SIGMA_ABC_AGENT_RUNTIME_REQUIRED.md
SIGMA_ABC_CENTER_SECTOR_PILOT_REPORT.md
SIGMA_ABC_HUMAN_APPROVAL_REQUIRED.md
SIGMA_ABC_HUMAN_APPROVAL_REQUIRED_FOR_IBP.md
SIGMA_ABC_HYPOTHESIS_PRE_IBP_RUNTIME_RETRY_AFTER_LIMIT_REPORT.md
SIGMA_ABC_HYPOTHESIS_PRE_IBP_RUN_REPORT.md
SIGMA_ABC_PAIR_KERNEL_FUSION_PILOT_REPORT.md
SIGMA_ABC_SAFE_PREFUSION_RUN_REPORT.md
SIGMA_ABC_STAGE011_PENDING_REVIEW_REPORT.md
SIGMA_ABC_STAGE011_REVIEW_DEBT_REPORT.md
SIGMA_ABC_STAGE011_RUNTIME_RETRY_AFTER_LIMIT_REPORT.md
SIGMA_ABC_STAGE_RUN_AFTER_REVIEW_HARDENING_REPORT.md
STAGE_012C_PROMOTION_PRE_AUDIT.md
```

6 files into `docs/devlog/audits/`:

```text
REPO_AUDIT.md
PROFILE_RUNNER_AUDIT.md
PROFILE_RUNNER_DRY_RUN.md
SCIENTIFIC_REVIEWER_AUDIT.md
PONYTAIL_AUDIT_2026-07-01.md
PLAN.md
```

7 files into `archive/local_runs/`:

```text
AUTONOMOUS_LOOP_RUN_REPORT.md
PROFILE_RUNNER_AUDIT.md                <- regenerated by pytest; re-archived
PROFILE_RUNNER_DRY_RUN.md              <- regenerated by pytest; re-archived
SCHEMA_VALIDATION_RESULT.json
SIGMA_ABC_CENTER_SECTOR_PILOT_REPORT.md <- regenerated by pytest; re-archived
SIGMA_ABC_PAIR_KERNEL_FUSION_PILOT_REPORT.md
SIGMA_ABC_SAFE_PREFUSION_RUN_REPORT.md  <- regenerated by pytest; re-archived
```

Note: `PROFILE_RUNNER_AUDIT.md`, `PROFILE_RUNNER_DRY_RUN.md`,
`SIGMA_ABC_CENTER_SECTOR_PILOT_REPORT.md`,
`SIGMA_ABC_PAIR_KERNEL_FUSION_PILOT_REPORT.md`,
`SIGMA_ABC_SAFE_PREFUSION_RUN_REPORT.md`,
`AUTONOMOUS_LOOP_RUN_REPORT.md` are
**regenerated** by `scripts/run_autonomous_loop.py` at runtime
(REPO_ROOT-anchored writes). The pytest suite exercises the runner
multiple times, so these files reappear at the root after each
pytest run. They were re-archived as a final step; future pytest
runs will re-emit them. This is a known runner behavior, not a
bug.

## Files Created

```text
docs/REPO_LAYOUT.md
docs/CLASSIFICATION_MANIFEST.md
docs/user_guide/QUICKSTART.md
case_studies/sigma_abc/README.md
REPO_CLASSIFICATION_PRE_AUDIT.md        (kept at root, audit-of-record)
REPO_REORGANIZATION_REPORT.md          (this file)
```

`README.md` updated with pointers to `docs/REPO_LAYOUT.md` and
`docs/user_guide/QUICKSTART.md`.

## Folders Created (with `.gitkeep` so git preserves them)

```text
docs/
docs/devlog/
docs/devlog/loop_engine/
docs/devlog/sigma_abc/
docs/devlog/audits/
docs/user_guide/
case_studies/
case_studies/sigma_abc/
case_studies/sigma_abc/project/
case_studies/sigma_abc/profiles/
case_studies/sigma_abc/identities/
case_studies/sigma_abc/benchmarks/
case_studies/sigma_abc/policies/
case_studies/sigma_abc/conventions/
case_studies/sigma_abc/reports_curated/
archive/
archive/local_runs/
```

## Files Ignored / Deleted (caches + OS metadata)

Removed from working tree at the end of Step 4:

```text
$ find . -maxdepth 6 -type d -name "__pycache__" -not -path "./.git/*" -exec rm -rf {} +
$ find . -maxdepth 6 -type d -name ".pytest_cache" -not -path "./.git/*" -exec rm -rf {} +
$ find . -maxdepth 6 -type f -name ".DS_Store" -not -path "./.git/*" -delete
```

These regenerated during pytest (because pytest imports modules),
but they are now gitignored.

## `.gitignore` Updated

Final contents of `.gitignore`:

```text
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
*.pyc
*.pyo
*.aux
*.log
*.out
*.toc
.DS_Store
.env
__MACOSX/
.claude/

# Generated artifacts
autonomous_runs/
reports/

# Local-only machine-specific runtime config
agents/runtime.local.yaml

# Archived local-run snapshots
archive/local_runs/

# Loop-engine bytecode caches from prior runs
loop_engine/__pycache__/
schemas/__pycache__/
scripts/__pycache__/
tests/__pycache__/
```

## Files Intentionally Left in Place

```text
README.md                              <- public root, updated with pointers
AGENTS.md                              <- public root, agent guidance
LICENSE
loop_config.json
pyproject.toml

loop_engine/, scripts/, schemas/,
templates/, skill/, tests/             <- PUBLIC_CORE reusable framework

agents/runtime.local.example.yaml       <- PUBLIC template
agents/runtime.local.yaml               <- LOCAL_PRIVATE (gitignored)

policies/                              <- PUBLIC_CORE reusable policies
examples/, smoke_projects/             <- PUBLIC_EXAMPLE
sigma_abc/, profiles/,
benchmarks/, identities/,
projects/sigma_abc/                    <- CASE_STUDY_SIGMA_ABC (root)
                                        (migration target documented)

autonomous_runs/, reports/             <- GENERATED_ARTIFACT (root; gitignored)

REPO_CLASSIFICATION_PRE_AUDIT.md       <- audit-of-record (root)
```

`sigma_abc/` and the `sigma_abc_*` profile/identity/benchmark
files were intentionally **NOT moved** in this pass — moving them
would break the trust-stack tests that reference root-relative paths
(`tests/test_sigma_abc_*`). Their planned landing zone
(`case_studies/sigma_abc/`) is documented in
`case_studies/sigma_abc/README.md`.

## Verification

### pytest

```text
$ python3 -m pytest -q
... 177 passed, 1 warning in 38.15 s
```

Exit code 0. Failure count 0. All Loop 019R tests + 159 prior
tests + the smoke/runner tests all pass under the reorganized
layout.

### compileall

```text
$ python3 -m compileall loop_engine scripts tests
Listing 'loop_engine'...
Listing 'scripts'...
Listing 'tests'...
Compiling 'tests/test_sigma_abc_hypothesis_pre_ibp_profile.py'...
Compiling 'tests/test_sigma_abc_loop_candidate_preparation.py'...
Compiling 'tests/test_sigma_abc_loop_candidate_promotion_profile.py'...
(no errors)
```

### Forbidden Artifact Scan

```text
$ find . -maxdepth 7 -type f \
    \( -name "*stage_013*" -o -name "*sigma_abc_013_*" \
       -o -name "*tensorial_ibp*" -o -name "*total_derivative*" \
       -o -name "*global_pre_ibp*" -o -name "*promoted_candidate_manifest*" \
       -o -name "*stage_012c_loop_orbit_canonicalization_promotion*" \) \
    -not -path "./.git/*" -not -path "./archive/*"
-> (no output)
```

Clean. No 013 / IBP / total-derivative / promotion artifacts.

### Root Cleanliness After Reorganization

```text
$ ls *.md *.json
AGENTS.md
README.md
REPO_CLASSIFICATION_PRE_AUDIT.md
loop_config.json
```

4 public-core files. Plus all directories. **No top-level
`LOOP_*.md`, `SIGMA_ABC_*.md`, `AUTONOMOUS_LOOP_RUN_REPORT.md`,
`*_AUDIT.md`, `STAGE_*.md`, `PONYTAIL_AUDIT_*.md`, `PLAN.md`,
`SCHEMA_VALIDATION_RESULT.json` remain at the root.**

(After pytest, 5 runner-emitted files reappear at root and were
re-archived. They regenerate on every pytest run; this is
documented behavior, not a regression.)

## Remaining Cleanup Tasks (Documented For Future Passes)

1. **Migrate `sigma_abc/` case-study tree to
   `case_studies/sigma_abc/`.** This requires updating:
   - `loop_engine/config.py` path lookup for the `sigma_abc`
     profile, loop spec, and identity library.
   - All `tests/test_sigma_abc_*` paths.
   - Verifying that all 177 tests still pass and that
     `completion_matrix`, `identity_traceability`, and `pre_run_gate`
     pass on the migrated case study.

2. **Decide whether to commit `agents/runtime.local.yaml`.**
   Currently gitignored. The user's machine-specific overrides
   (Codex binary path, real-quota fallback) live there. If the
   repo is meant to be portable, this file should not be committed
   and the `.gitignore` rule stays. If the repo is meant to be a
   per-machine checkpoint of a known setup, the `.gitignore` rule
   should be removed and the example file merged into the runtime
   file.

3. **Review `PROFILE_RUNNER_AUDIT.md` and
   `PROFILE_RUNNER_DRY_RUN.md` re-emission.** They are written
   each pytest run. Options:
   - (a) leave the runner behavior as-is, route outputs to
     `archive/local_runs/`, and re-archive in the report-writing
     step.
   - (b) modify `scripts/run_autonomous_loop.py` to write into
     `archive/local_runs/` directly when `REPO_ROOT` is the cwd.
     This would make report-writing deterministic without
     post-pytest re-archiving.

4. **Reviewer quota handling.** `LOOP_019R_QUOTA_STILL_ACTIVE_REPORT.md`
   records that Codex CLI's user-level quota is still active on
   this machine. Until that clears, throughput runs cannot
   produce full `ScientificMetaReviewer` verdicts. This is
   orthogonal to repo hygiene and lives in `docs/devlog/loop_engine/`.

## Boundary Constraints Honored

- Did not modify loop behavior.
- Did not modify `sigma_abc/` physics.
- Did not run `autonomous_loop.py` except as a side effect of
  pytest test cases (the existing `tests/test_*` suite runs the
  runner via `subprocess`; that is a pre-existing test behavior,
  not a new runner invocation triggered by this pass).
- Did not start 012C / 013 / IBP / total derivative.
- Did not claim full tensorial sigma_abc correctness.
- Did not weaken pre_run_gate / freeze_preconditions /
  human_signoff.
- Did not replace real reviewer with stub.
- Did not bypass L2_FULL_PANEL review.
- Did not convert missing physical artifacts into success.

## Permanent Caveat Preserved

```text
DCProjectionTo1D -> INHERITED_PASS, not direct full tensorial DC-series PASS.
```

This caveat is preserved in every frozen `validation_summary.caveats`
entry that still exists (e.g., the `010_pair_kernel_fusion_pilot`
frozen checkpoint), in `identities/sigma_abc.default_identities.yaml`
(`DC inherited caveat` identity), and in
`docs/user_guide/QUICKSTART.md`.
