# Sigma ABC 012AB Artifact Contract Patch Report

## Scope

Branch intent: `sigma_abc_012ab_artifact_contract_patch`.

This patch only standardizes the upstream artifact contract required by
`sigma_abc_012c_real_loop_candidate_preparation`. It does not promote a loop
candidate, does not start Stage 013, does not introduce tensorial IBP or total
derivative reduction, and does not claim full tensorial
`sigma_{mu alpha beta}` correctness.

## Code Changes

- Stage 012A now exports the standard loop-inventory contract:
  - `output/loop_sector_ledger.csv`
  - `output/loop_orbit_inventory.json`
  - `output/loop_raw_sector_table.wl`
  - `validation/loop_inventory_validation.json`
- Stage 012B now exports the standard loop-hypothesis contract:
  - `output/loop_hypothesis_ledger.json`
  - `output/loop_candidate_requirements.json`
  - `.loop/conjectures/conjecture_ledger.json`
  - `reports/loop_hypothesis_generation_summary.md`
- The 012C-preparation resolver now searches:
  - current run-root stage directories;
  - frozen checkpoint directories;
  - checkpoint manifests via `output_artifacts`, `input_snapshots`, and `files`;
  - copied input snapshots under `input_snapshots/stage012a/` and
    `input_snapshots/stage012b/`.

## Validation Evidence

```text
python3 -m pytest -q
119 passed, 1 warning

python3 -m compileall loop_engine scripts tests
PASS

python3 -m pytest -q tests/test_sigma_abc_loop_candidate_preparation.py
6 passed, 1 warning
```

## Current Run-Root Preparation Result

After the patch, rerunning `sigma_abc_loop_candidate_preparation` produced:

```text
overall_gate: FAIL
Stage012AArtifactPresent: True
Stage012BArtifactPresent: False
UsesStage012ALoopLedger: True
UsesStage012BHypothesisLedger: False
PreparationStatus: BLOCKED_MISSING_STAGE012AB_ARTIFACT_CONTRACT
NoCandidatePromoted: True
NoIBPStarted: True
NoTotalDerivativeIntroduced: True
DecisionAction: DO_NOT_FREEZE
FreezeAllowed: False
```

This is the intended safety behavior for the current workspace: Stage 012A
artifacts are now discoverable, but Stage 012B artifacts are still missing, so
the preparation stage must not freeze.

## Throughput Attempt

A safe throughput run was attempted to regenerate 011/012A/012B without
promotion:

```text
python3 scripts/run_autonomous_loop.py \
  --project sigma_abc \
  --profile sigma_abc_hypothesis_pre_ibp_throughput \
  --from-current-checkpoint \
  --auto-patch \
  --write-digests \
  --max-stages 3
```

It generated Stage 011 and Stage 012A artifacts, but the real
`ScientificMetaReviewer` command for Stage 012A hung without producing
`stdout.txt`, `stderr.txt`, or `exit_code.txt`. The run was interrupted manually
to avoid an indefinite wait. No Stage 012B was generated by that interrupted
run.

## Scientific Boundary

Allowed claim:

```text
012AB artifact contract support is implemented and tested. The 012C-prep gate
correctly resolves Stage 012A artifacts and blocks when Stage 012B artifacts are
missing.
```

Not allowed:

```text
012C candidate promotion is ready.
Stage 013 can start.
Tensorial IBP or total-derivative reduction has started.
Full tensorial sigma_abc correctness is established.
```

## Next Safe Step

Resolve the real-agent runtime hang or run the low-risk Stage 012B generation
after the reviewer runtime is available. Then rerun:

```text
python3 scripts/run_autonomous_loop.py \
  --project sigma_abc \
  --profile sigma_abc_loop_candidate_preparation \
  --from-current-checkpoint \
  --auto-patch \
  --write-digests \
  --max-stages 1
```

The required gate for the next successful preparation run is:

```text
Stage012AArtifactPresent -> True
Stage012BArtifactPresent -> True
UsesStage012ALoopLedger -> True
UsesStage012BHypothesisLedger -> True
PreparationStatus -> READY_FOR_012C_PROMOTION_RETRY
OverallGate -> PASS
NoCandidatePromoted -> True
NoIBPStarted -> True
NoTotalDerivativeIntroduced -> True
```
